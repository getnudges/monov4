query GetClientByPhoneNumber($phoneNumber: String!) {
  clientByPhoneNumber(phoneNumber: $phoneNumber) {
    locale
    subscription {
      status
    }
    subscribers {
      nodes {
        id
        locale
      }
    }
  }
}
