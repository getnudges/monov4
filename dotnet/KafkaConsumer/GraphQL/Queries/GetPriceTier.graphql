query GetPriceTier($id: ID!) {
  priceTier(id: $id) {
    id
    planId
    createdAt
    description
    duration
    iconUrl
    name
    planId
    price
    foreignServiceId
    status
    plan {
      foreignServiceId
    }
  }
}
