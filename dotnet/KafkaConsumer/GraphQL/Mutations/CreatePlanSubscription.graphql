mutation CreatePlanSubscription($input: CreatePlanSubscriptionInput!) {
  createPlanSubscription(input: $input) {
    errors {
      ... on Error {
        message
      }
    }
    planSubscription {
      id
      client {
        id
      }
    }
  }
}
