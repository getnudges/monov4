query GetPlanSubscription($planSubscriptionId: ID!) {
    planSubscription(id: $planSubscriptionId) {
        startDate
        endDate
        clientId
        client {
            id
            phoneNumber
        }
    }
}
